# Exemplos de nginx como proxy reverso

Essa aplicação tem como objetivo demonstrar uma implementação de proxy reverso com nginx.

## Rodar a aplicação:

Rode o comando docker abaixo no diretorio principal

```
docker-compose up --build
```